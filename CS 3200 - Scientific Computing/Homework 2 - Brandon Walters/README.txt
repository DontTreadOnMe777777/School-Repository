Howdy!

Questions are written iteratively, so in each specific question only the new bits are commented/documented.

Make sure to get the polyinterp and barylag function files!

Some error outputs are returned as print statements, so keep an eye out!